package class039;

import java.util.ArrayList;

// 含有嵌套的表达式求值
https://leetcode.com/problems/basic-calculator/
https://leetcode.com/problems/basic-calculator-ii/
https://leetcode.com/problems/basic-calculator-iii/

public class Code01_BasicCalculatorIII {

	# Basic Calculator - 三种解法完整对比

## 题目对应

| 解法 | 适用题目 | LeetCode |
|------|---------|----------|
| **解法1** | 只有 +/- 和括号 | 224. Basic Calculator |
| **解法2** | 有 +/-/*/// 无括号 | 227. Basic Calculator II |
| **解法3** | 有 +/-/*/// 和括号 | 772. Basic Calculator III |

---

## 解法1: 递归处理括号（只有 +/- 和括号）

### 核心思路
- 用递归处理括号
- 只需处理加减法（优先级相同）
- numbers 存数字，operations 存运算符
- 最后统一计算

### 完整代码
```java
class Solution {
    int where;
    
    public int calculate(String s) {
        return f(s.toCharArray(), 0);
    }
    
    private int f(char[] s, int i) {
        int n = s.length;
        int cur = 0;
        List<Integer> numbers = new ArrayList<>();
        List<Character> operations = new ArrayList<>();
        
        while (i < n && s[i] != ')') {
            if (s[i] >= '0' && s[i] <= '9') {
                cur = cur * 10 + s[i++] - '0';
            } else if (s[i] == ' ') {
                i++;
            } else if (s[i] == '(') {
                cur = f(s, i + 1);       // 递归计算括号内容
                i = where + 1;           // where 指向 ')'，+1 跳过
            } else {  // '+' 或 '-'
                numbers.add(cur);
                operations.add(s[i++]);
                cur = 0;
            }
        }
        
        numbers.add(cur);  // 保存最后一个数字
        where = i;         // 更新全局位置
        return compute(numbers, operations);
    }
    
    private int compute(List<Integer> numbers, List<Character> operations) {
        int res = numbers.get(0);
        for (int i = 1; i < numbers.size(); i++) {
            res += operations.get(i - 1) == '+' ? numbers.get(i) : -numbers.get(i);
        }
        return res;
    }
}
```

### 特点
- ✅ **逻辑简单**: 只处理加减，无需考虑优先级
- ✅ **递归自然**: 括号递归处理很直观
- ✅ **代码清晰**: numbers 和 operations 一一对应
- ❌ **仅适用**: 只能处理 +/- 和括号

### 复杂度
- **Time**: O(n)
- **Space**: O(n)

---

## 解法2: 单栈处理优先级（有 */// 无括号）

### 核心思路
- 用 `lastOp` 记录上一个运算符
- 遇到 +/- → 存入栈（带符号）
- 遇到 */// → 立即和栈顶计算
- 最后栈内求和

### 完整代码
```java
class Solution {
    public int calculate(String s) {
        return f(s.toCharArray(), 0);
    }
    
    private int f(char[] s, int i) {
        Stack<Integer> numbers = new Stack<>();
        int cur = 0;
        char lastOp = '+';  // 记录上一个运算符
        
        while (i < s.length) {
            if (s[i] >= '0' && s[i] <= '9') {
                cur = cur * 10 + s[i] - '0';
            }
            
            // 遇到运算符或最后一个字符
            if (s[i] != ' ' && (s[i] < '0' || s[i] > '9') || i == s.length - 1) {
                // 根据上一个运算符处理当前数字
                if (lastOp == '+') {
                    numbers.push(cur);
                } else if (lastOp == '-') {
                    numbers.push(-cur);
                } else if (lastOp == '*') {
                    numbers.push(numbers.pop() * cur);  // 立即计算
                } else if (lastOp == '/') {
                    numbers.push(numbers.pop() / cur);  // 立即计算
                }
                
                if (i < s.length - 1) {
                    lastOp = s[i];  // 更新运算符
                }
                cur = 0;
            }
            i++;
        }
        
        // 栈内求和
        int res = 0;
        while (!numbers.isEmpty()) {
            res += numbers.pop();
        }
        return res;
    }
}
```

### 特点
- ✅ **处理优先级**: */// 立即计算，+/- 延迟求和
- ✅ **单栈高效**: 只用一个栈
- ✅ **代码简洁**: 逻辑直观
- ❌ **无括号**: 不支持括号

### 复杂度
- **Time**: O(n)
- **Space**: O(n)

---

## 解法3: 递归 + 优先级（完整版）

### 核心思路
- 递归处理括号（同解法1）
- push 方法处理优先级（核心创新）
- 判断**上一个运算符**决定是否立即计算

### 完整代码
```java
class Solution {
    int where;
    
    public int calculate(String s) {
        where = 0;
        return f(s.toCharArray(), 0);
    }
    
    private int f(char[] s, int i) {
        List<Integer> nums = new ArrayList<>();
        List<Character> ops = new ArrayList<>();
        int cur = 0;
        
        while (i < s.length && s[i] != ')') {
            if (s[i] >= '0' && s[i] <= '9') {
                cur = cur * 10 + s[i++] - '0';
            } else if (s[i] == ' ') {
                i++;
            } else if (s[i] != '(') {
                push(nums, ops, cur, s[i++]);
                cur = 0;
            } else {  // '('
                cur = f(s, i + 1);
                i = where + 1;
            }
        }
        
        where = i;
        push(nums, ops, cur, '+');  // 虚拟 '+' 处理最后数字
        return compute(nums, ops);
    }
    
    private void push(List<Integer> nums, List<Character> ops, int cur, char op) {
        int n = nums.size();
        
        // ✅ 关键：判断上一个运算符（不是当前 op）
        if (n == 0 || ops.get(n - 1) == '+' || ops.get(n - 1) == '-') {
            // 上一个是低优先级或没有：直接加入
            nums.add(cur);
            ops.add(op);
        } else {
            // 上一个是高优先级：立即计算
            int lastNum = nums.get(n - 1);
            if (ops.get(n - 1) == '*') {
                nums.set(n - 1, lastNum * cur);
            } else {  // '/'
                nums.set(n - 1, lastNum / cur);
            }
            ops.set(n - 1, op);  // 更新运算符
        }
    }
    
    private int compute(List<Integer> nums, List<Character> ops) {
        int res = nums.get(0);
        for (int i = 1; i < nums.size(); i++) {
            res += ops.get(i - 1) == '+' ? nums.get(i) : -nums.get(i);
        }
        return res;
    }
}
```

### 特点
- ✅ **功能最全**: 支持所有运算符和括号
- ✅ **封装良好**: push 方法封装优先级处理
- ✅ **递归清晰**: 括号处理自然
- ✅ **通用性强**: 可扩展到更多运算符

### 复杂度
- **Time**: O(n)
- **Space**: O(n)

---

## 核心对比

| 对比项 | 解法1 (LeetCode 224) | 解法2 (LeetCode 227) | 解法3 (LeetCode 772) |
|--------|---------------------|---------------------|---------------------|
| **支持括号** | ✅ | ❌ | ✅ |
| **支持 */// ** | ❌ | ✅ | ✅ |
| **优先级处理** | 不需要 | lastOp 延迟 | push 方法封装 |
| **数据结构** | List + List | Stack | List + List |
| **关键变量** | where (位置) | lastOp (运算符) | where + push |
| **代码复杂度** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **理解难度** | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **通用性** | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

---

## 关键技术对比

### 1. 括号处理

#### 解法1 和 解法3（递归）
```java
else if (s[i] == '(') {
    cur = f(s, i + 1);    // 递归计算括号内
    i = where + 1;        // where 记录 ')'，+1 跳过
}
```

**特点**:
- ✅ 递归天然处理嵌套
- ✅ 代码简洁
- ✅ 逻辑清晰

#### 解法2（无括号）
```java
// 不支持括号
```

---

### 2. 优先级处理

#### 解法1（无需处理）
```java
// 只有 +/-，优先级相同，直接计算
return compute(numbers, operations);
```

#### 解法2（lastOp 延迟）
```java
char lastOp = '+';  // 记录上一个运算符

// 遇到数字时，用上一个运算符处理
if (lastOp == '+') numbers.push(cur);
else if (lastOp == '-') numbers.push(-cur);
else if (lastOp == '*') numbers.push(numbers.pop() * cur);  // 立即计算
else if (lastOp == '/') numbers.push(numbers.pop() / cur);

lastOp = 当前运算符;  // 更新
```

**特点**:
- ✅ "延迟处理"思想
- ✅ 用上一个运算符处理当前数字
- ✅ 高优先级立即计算

#### 解法3（push 封装）
```java
private void push(List<Integer> nums, List<Character> ops, int cur, char op) {
    if (n == 0 || ops.get(n - 1) == '+' || ops.get(n - 1) == '-') {
        // 上一个是低优先级：直接加入
        nums.add(cur);
        ops.add(op);
    } else {
        // 上一个是高优先级：立即计算
        nums.set(n - 1, lastNum * cur);  // 或 /
        ops.set(n - 1, op);
    }
}
```

**特点**:
- ✅ 封装优先级逻辑
- ✅ 主函数更清晰
- ✅ 判断上一个运算符（关键！）

---

### 3. 位置管理

#### 解法1 和 解法3（全局 where）
```java
int where;  // 全局变量记录位置

// 递归返回后
i = where + 1;  // 获取递归处理后的位置
```

**特点**:
- ✅ 位置在递归间传递
- ⚠️ 需要全局变量

#### 解法2（局部 i）
```java
while (i < s.length) {
    i++;  // 自己管理位置
}
```

**特点**:
- ✅ 无需全局变量
- ✅ 逻辑独立

---

## 数据结构对比

### 解法1 和 解法3
```java
List<Integer> numbers = [3, 5, 2]
List<Character> operations = ['+', '-']

含义:
  3 + 5 - 2
```

**关系**: `numbers[i]` 和 `operations[i]` 对应

---

### 解法2
```java
Stack<Integer> numbers = [3, 5, -2]

含义:
  3 + 5 + (-2) = 6
```

**关系**: 所有数字已经**带符号**，直接求和

---

## 易错点对比

### 解法1 易错点

| 易错点 | 错误代码 | 正确代码 |
|--------|---------|---------|
| 忘记保存最后数字 | `return compute(...)` | `numbers.add(cur); return compute(...)` |
| where 使用错误 | `i = where` | `i = where + 1` |
| 忘记空格处理 | 缺少 | `else if (s[i] == ' ') i++;` |
| 全局变量冲突 | `List numbers;` 全局 | 改成局部变量 |

---

### 解法2 易错点

| 易错点 | 错误代码 | 正确代码 |
|--------|---------|---------|
| lastOp 在循环内 | `while(...) { char lastOp='+'; }` | `char lastOp='+'; while(...)` |
| 忘记更新 lastOp | 缺少 | `lastOp = s[i];` |
| 最后数字丢失 | 循环后不处理 | 加 `i == s.length - 1` 判断 |
| 字符比较错误 | `s[i] <= 9` | `s[i] <= '9'` |
| 条件优先级 | `... && ... || ...` | 加括号或调整顺序 |

---

### 解法3 易错点

| 易错点 | 错误代码 | 正确代码 |
|--------|---------|---------|
| push 判断错误 | `if (op == '+')` | `if (ops.get(n-1) == '+')` |
| 忘记空格处理 | `else if (s[i] != '(')` | 先处理空格 |
| where 使用错误 | `i = where` | `i = where + 1` |
| ops 更新方式 | `ops.add(op)` 混乱 | 根据分支正确更新 |

---

## 核心技术点对比

### 1. 递归处理括号

#### 解法1 和 解法3
```java
// 模式
if (s[i] == '(') {
    cur = f(s, i + 1);  // 递归
    i = where + 1;      // 跳过 ')'
}

// where 的作用
递归内部: where = i (指向 ')')
递归外部: i = where + 1 (跳过 ')')
```

#### 解法2
```java
// 不支持括号
```

---

### 2. 优先级处理

#### 解法1
```java
// 无需处理，+/- 优先级相同
```

#### 解法2
```java
// "延迟处理"模式
char lastOp = '+';

// 用上一个运算符处理当前数字
if (lastOp == '*' || lastOp == '/') {
    立即计算  // 高优先级
} else {
    延迟加入  // 低优先级
}

lastOp = 当前运算符;  // 延迟到下一次
```

#### 解法3
```java
// "封装判断"模式
private void push(..., char op) {
    // 判断上一个运算符 ops[n-1]
    if (ops[n-1] == '*' || ops[n-1] == '/') {
        立即计算  // 高优先级
    } else {
        直接加入  // 低优先级
    }
}
```

---

### 3. 最后数字处理

#### 解法1
```java
// 循环后直接加入
numbers.add(cur);
```

#### 解法2
```java
// 在条件中加入 i == s.length - 1
if (...运算符... || i == s.length - 1) {
    处理数字
}
```

#### 解法3
```java
// 用虚拟运算符触发
push(nums, ops, cur, '+');  // 巧妙！
```

---

## 适用场景

### 解法1: LeetCode 224
```
输入: "1 + 1"              → 2
     "2-1 + 2"            → 3
     "(1+(4+5+2)-3)+(6+8)" → 23
     "1-(2+3)"            → -4
```

**特征**: 
- ✅ 只有 +/-
- ✅ 有括号
- ✅ 有空格

---

### 解法2: LeetCode 227
```
输入: "3+2*2"      → 7
     "3/2"        → 1
     "3+5/2"      → 5
     " 3+5 / 2 "  → 5
```

**特征**:
- ✅ 有 +/-/*/÷
- ❌ 无括号
- ✅ 有空格

---

### 解法3: LeetCode 772
```
输入: "1 + 1"                → 2
     "6-4 / 2"              → 4
     "2*(5+5*2)/3+(6/2+8)"  → 21
     "(2+6*3+5-(3*14/7+2)*5)+3" → -12
```

**特征**:
- ✅ 有 +/-/*/÷
- ✅ 有括号
- ✅ 有空格
- ✅ 最复杂

---

## 记忆口诀

### 解法1
```
只加减，递归括号，存数算最后
```

### 解法2
```
有乘除，记上符，高优先即刻算
```

### 解法3
```
全功能，递归括号，push判上符
```

---

## 推荐学习路径
```
第1周: 掌握解法1
  → LeetCode 224
  → 理解递归处理括号
  → 理解 where 的作用

第2周: 掌握解法2
  → LeetCode 227
  → 理解 lastOp 延迟处理
  → 理解优先级的本质

第3周: 掌握解法3
  → LeetCode 772
  → 结合解法1和解法2
  → 理解 push 封装思想
```

---

## 关键区别总结

| 核心点 | 解法1 | 解法2 | 解法3 |
|--------|-------|-------|-------|
| **括号** | 递归 | 不支持 | 递归 |
| **优先级** | 无 | lastOp 延迟 | push 判断 |
| **判断对象** | - | 上一个 lastOp | 上一个 ops[n-1] |
| **数据结构** | List+List | Stack | List+List |
| **最后数字** | 直接 add | 条件触发 | 虚拟 '+' |
| **位置管理** | where 全局 | i 局部 | where 全局 |

---

## 你的进步

从你的三个版本可以看出：
1. ✅ **解法1**: 理解了递归处理括号
2. ✅ **解法2**: 理解了优先级的延迟处理
3. ✅ **解法3**: 结合了两者，封装更好

**非常棒的学习过程！** 👍

现在你应该对计算器问题有全面的理解了！

}



